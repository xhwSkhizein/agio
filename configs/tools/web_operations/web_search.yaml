type: tool
name: web_search
description: "Search the web for information using Serper API"
tool_name: web_search
dependencies:
  citation_source_store: citation_store_mongodb  # Reference to citation store for storing search results
params:
  # Requires SERPER_API_KEY environment variable
  # timeout_seconds: 30
  # max_results: 10
enabled: true
tags:
  - search
  - web
  - research
