type: tool
name: send_email
description: "Send email notifications"
tool_type: class
class_path: agio.tools.example.email.SendEmailTool
class_params:
  smtp_server: ${SMTP_SERVER}
  smtp_port: 587
  username: ${SMTP_USERNAME}
  password: ${SMTP_PASSWORD}
  from_address: support@example.com
enabled: true
tags:
  - email
  - notification
  - communication
