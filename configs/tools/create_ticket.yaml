type: tool
name: create_ticket
description: "Create a support ticket in the ticketing system"
tool_type: class
class_path: agio.tools.example.ticketing.CreateTicketTool
class_params:
  api_url: ${TICKETING_API_URL}
  api_key: ${TICKETING_API_KEY}
  default_priority: medium
enabled: true
tags:
  - ticketing
  - support
  - integration
