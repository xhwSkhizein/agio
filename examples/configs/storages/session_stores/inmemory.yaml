type: session_store
name: inmemory_session_store
description: "In-memory session store for testing and development"
enabled: false  # Disabled: using MongoDB for persistence
tags:
  - testing
  - inmemory
  - development

backend:
  type: inmemory

enable_indexing: false
batch_size: 100
