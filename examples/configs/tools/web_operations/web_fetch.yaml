type: tool
name: web_fetch
description: "Fetch and process web content with optional LLM processing"
tool_name: web_fetch
dependencies:
  llm_model: deepseek  # Reference to model config for content summarization
  citation_source_store: citation_store_mongodb  # Reference to citation store for index-based fetching
params:
  # timeout_seconds: 30
  # max_content_length: 4096
  # max_retries: 1
  # wait_strategy: "domcontentloaded"
  # max_size_mb: 10.0
  # headless: true
  # save_login_state: false
  # user_data_dir: "chrome_user_data"
  # browser_launch_timeout: 30
enabled: true
tags:
  - web
  - content
  - llm-enhanced
