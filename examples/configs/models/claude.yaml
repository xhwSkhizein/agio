type: model
name: claude
description: "MiniMax-M2 anthropic api"
provider: anthropic
model_name: MiniMax-M2
base_url: https://api.minimaxi.com/anthropic
api_key: "{{ env.MINIMAX_API_KEY }}"
temperature: 0.7
max_tokens: 4096
timeout: 60
enabled: false  # Disabled: only using deepseek for testing
tags:
  - anthropic
  - minimax-m2
